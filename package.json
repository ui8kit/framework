{
  "name": "@ui8kit/framework",
  "version": "0.0.1",
  "description": "Next generation UI system for React & HTML5/CSS3 - type-safe components with semantic static generation",
  "private": true,
  "license": "GPL-3.0",
  "keywords": ["ui", "react", "html5", "css3", "semantic", "static-site", "monorepo", "turbo"],
  "type": "module",
  "author": {
    "name": "buildy",
    "url": "https://ui.buildy.tw"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bunx turbo run dev",
    "build": "bunx turbo run build",
    "preview": "bunx turbo run preview",
    "test": "bunx turbo run test",
    "lint": "bunx turbo run lint",
    "lint:dsl:engine": "bun run packages/lint/src/cli/validate-dsl.ts apps/engine/src",
    "generate": "bunx turbo run generate --filter=./apps/docs --filter=./apps/web",
    "generate:docs": "bunx turbo run generate --filter=./apps/docs",
    "generate:web": "bunx turbo run generate --filter=./apps/web",
    "generate:engine": "bunx turbo run generate --filter=./apps/engine",
    "copy-templates-to-dev": "bun run scripts/copy-templates-to-dev.ts",
    "copy-templates-to-test": "TARGET_APP=test bun run scripts/copy-templates-to-dev.ts",
    "bundle-data": "bun run scripts/bundle-data.ts",
    "alias-data": "bun run scripts/configure-data-alias.ts",
    "validate:data-bundle": "bun run scripts/validate-data-bundle.ts",
    "pipeline-app": "bun run scripts/pipeline-app.ts",
    "pipeline:dev": "bun run scripts/pipeline-app.ts all --target dev --domain website --data-mode local",
    "pipeline:dev:local": "bun run scripts/pipeline-app.ts all --target dev --domain website --data-mode local",
    "pipeline:dev:shared": "bun run scripts/pipeline-app.ts all --target dev --domain website --data-mode shared",
    "pipeline:test": "bun run scripts/pipeline-app.ts all --target test --domain website --data-mode local",
    "pipeline:test:website": "bun run scripts/pipeline-app.ts all --target test --domain website --data-mode local",
    "pipeline:test:docs": "bun run scripts/pipeline-app.ts all --target test --domain docs --data-mode local",
    "pipeline:test:examples": "bun run scripts/pipeline-app.ts all --target test --domain examples --data-mode local",
    "pipeline:test:dashboard": "bun run scripts/pipeline-app.ts all --target test --domain dashboard --data-mode local",
    "scaffold-app": "bun run scripts/scaffold-app.ts",
    "install-app": "bash scripts/installer.sh",
    "clean:engine": "bash scripts/clean-engine.sh",
    "clean:workspace": "bash scripts/clean-workspace.sh",
    "sync-app": "bun run scripts/pipeline-app.ts sync --data-mode local",
    "sync-dev": "bun run scripts/pipeline-app.ts sync --target dev --domain website --data-mode local",
    "sync-test": "bun run scripts/pipeline-app.ts sync --target test --domain website --data-mode local",
    "sync:website": "bun run scripts/pipeline-app.ts sync --domain website --data-mode local",
    "sync:docs": "bun run scripts/pipeline-app.ts sync --domain docs --data-mode local",
    "sync:examples": "bun run scripts/pipeline-app.ts sync --domain examples --data-mode local",
    "sync:dashboard": "bun run scripts/pipeline-app.ts sync --domain dashboard --data-mode local",
    "m": "bash scripts/engine.sh"

  },
  "packageManager": "bun@1.3.3",
  "devDependencies": {
    "serve": "^14.2.5",
    "turbo": "^2.5.6"
  }
}
