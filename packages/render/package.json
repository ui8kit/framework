{
  "name": "@ui8kit/render",
  "version": "0.1.0",
  "description": "React static renderer for UI8Kit - converts React components to HTML with data-class attributes",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed - source is used directly'",
    "dev": "echo 'Render runs on-demand via generate command'",
    "test": "bun test",
    "lint": "bunx tsc --noEmit",
    "typecheck": "bunx tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0",
    "react-router-dom": "^6.0.0 || ^7.0.0"
  },
  "devDependencies": {
    "bun": "^1.3.0",
    "typescript": "^5.8.0",
    "@types/bun": "^1.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
