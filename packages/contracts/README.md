# @ui8kit/contracts

Typed data contracts and context helpers shared across UI8Kit packages.

## Build

```bash
bun run build
```

## Exports

- `@ui8kit/contracts` -> runtime + types from `dist`
- `@ui8kit/contracts/source` -> source-level exports from `src`
