{
  "name": "@ui8kit/generator",
  "version": "0.1.0",
  "description": "Static site generator for UI8Kit - converts React to Liquid templates",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed - source is used directly'",
    "dev": "echo 'Generator runs on-demand via generate command'",
    "test": "bun test",
    "lint": "bunx tsc --noEmit",
    "typecheck": "bunx tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0"
  },
  "dependencies": {
    "glob": "^13.0.0",
    "liquidjs": "^10.10.0",
    "uncss": "^0.17.3"
  },
  "devDependencies": {
    "bun": "^1.3.0",
    "typescript": "^5.8.0",
    "@types/bun": "^1.0.0",
    "@types/react": "^19.0.0"
  }
}
