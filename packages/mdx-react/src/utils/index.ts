// Re-export from core for backwards compatibility
export {
  slugify,
  uniqueSlug,
  parseFrontmatter,
  extractToc,
  buildHierarchicalToc,
  extractExcerpt,
  parseMdxFile,
  scanDocsTree,
  flattenDocsTree,
  buildSidebarFromTree,
} from '../core'
