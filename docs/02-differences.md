# 2. Разница между Tailwind, PostCSS и UnCSS

## 🔍 Обзор инструментов

В современной веб-разработке используется цепочка инструментов для генерации и оптимизации CSS. Давайте разберемся в роли каждого.

## 🌀 Tailwind CSS - генератор утилит

### Что делает

Tailwind CSS - это **CSS фреймворк**, который генерирует тысячи утилитарных классов.

```css
/* Tailwind генерирует */
.w-1 { width: 0.25rem; }
.w-2 { width: 0.5rem; }
/* ... и так далее до w-96 ... */
.text-red-500 { color: rgb(239 68 68); }
.bg-blue-600 { background-color: rgb(37 99 235); }
```

### Проблема

- Генерирует **все возможные комбинации** цветов, размеров, отступов
- Итоговый файл: **2,959 строк** (127KB)
- 80% классов никогда не используются

### Когда использовать

- Для быстрой разработки компонентов
- Когда нужно много готовых утилит
- В активной разработке

## ⚙️ PostCSS - процессор CSS

### Что делает

PostCSS - это **платформа** для обработки CSS с плагинами.

```javascript
// postcss.config.mjs
module.exports = {
  plugins: [
    require('tailwindcss'),
    require('autoprefixer'),
    require('cssnano')
  ]
}
```

### Роль в цепочке

1. **Принимает** CSS/Tailwind код
2. **Обрабатывает** через плагины
3. **Выводит** готовый CSS

### Плагины

- `tailwindcss` - генерирует утилиты
- `autoprefixer` - добавляет vendor prefixes
- `cssnano` - минифицирует
- `uncss` - **удаляет неиспользуемые стили**

## 🎯 UnCSS - оптимизатор

### Что делает

UnCSS - это **PostCSS плагин**, который анализирует HTML и удаляет неиспользуемые CSS правила.

```javascript
// Пример использования
const uncss = require('uncss');

uncss(['index.html'], {
  raw: '.used-class { color: red; } .unused-class { color: blue; }'
}, (error, output) => {
  console.log(output); // Только .used-class
});
```

### Как работает

1. **Парсит HTML** через JSDOM (виртуальный браузер)
2. **Ищет все селекторы** в DOM дереве
3. **Сравнивает** с CSS правилами
4. **Удаляет** неиспользуемые

### Результат

**Вход**: 2,959 строк Tailwind CSS
**Выход**: 611 строк (79% экономии)

## 🔄 Цепочка обработки

```
Исходный код → Vite.js → Tailwind CSS → PostCSS → UnCSS → Минификация → Продакшн CSS
     ↓             ↓            ↓           ↓         ↓          ↓            ↓
   .tsx          сборка      генерация   обработка  очистка   сжатие      готовый
   файлы         модулей     утилит      плагинами  от HTML   CSS        CSS файл
```

### Подробно по шагам

1. **Vite.js** - собирает TypeScript/React в JavaScript
2. **Tailwind CSS** - конвертирует классы в CSS правила
3. **PostCSS** - применяет плагины (автопрефиксы, минификация)
4. **UnCSS** - удаляет неиспользуемые правила
5. **CSSNano** - финальная минификация

## 📊 Сравнение результатов

| Инструмент | Размер | Экономия | Метод анализа |
|------------|--------|----------|---------------|
| **Без оптимизации** | 2,959 строк | 0% | - |
| **PurgeCSS** | 1,200 строк | 40% | Текстовый поиск |
| **UnCSS** | 611 строк | **79%** | DOM анализ |
| **Manual** | 300 строк | 90% | Ручная работа |

## 🎯 Когда использовать каждый инструмент

### Tailwind CSS

```javascript
// Когда нужно быстро разработать
<div className="flex items-center justify-between p-4 bg-white shadow-md rounded-lg">
  <h1 className="text-xl font-bold text-gray-900">Заголовок</h1>
  <button className="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
    Кнопка
  </button>
</div>
```

**Плюсы:**
- Быстрая разработка
- Консистентный дизайн
- Много готовых утилит

### PostCSS

```javascript
// postcss.config.mjs
module.exports = {
  plugins: [
    require('tailwindcss'),
    require('uncss')({
      html: ['dist/**/*.html']
    }),
    require('cssnano')
  ]
}
```

**Плюсы:**
- Модульная архитектура
- Тысячи плагинов
- Гибкая настройка

### UnCSS

```javascript
// Только для продакшена
const uncss = require('uncss');

uncss(['dist/index.html'], {
  raw: fullTailwindCSS
}, (error, output, report) => {
  // output содержит только используемые стили
});
```

**Плюсы:**
- Максимальная оптимизация
- Реальный анализ DOM
- Идеальные метрики

## 🚀 Рекомендация

**Для production используйте все три инструмента вместе:**

1. **Tailwind** для удобной разработки
2. **PostCSS** как платформу
3. **UnCSS** для финальной оптимизации

Это дает идеальный баланс между скоростью разработки и производительностью.