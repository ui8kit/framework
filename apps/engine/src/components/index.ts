// Example DSL components (not used in main app, but kept as reference)
export { ProductCard } from './ProductCard';
export { ProductList } from './ProductList';
export { UserProfile } from './UserProfile';
export { Layout } from './Layout';
