# Очистка CSS в UI8Kit

Этот скрипт позволяет очистить CSS файл от неиспользуемых стилей на основе анализа HTML файла.

## Результаты

**Текущие результаты очистки:**
- **Оригинальный CSS**: 71,655 байт (2,958 строк)
- **Очищенный CSS**: 29,129 байт (610 строк)
- **Сокращение**: **79.4%** 🚀

## Использование

### Предварительные требования

1. Установите зависимости:
```bash
bun install
```

2. Убедитесь, что файлы существуют:
- `dist/html/index.html` - HTML файл для анализа
- `dist/html/assets/base.css` - CSS файл для очистки

### Запуск очистки

#### Через npm скрипт (рекомендуется)
```bash
bun run clean-css
```

#### Через прямой запуск
```bash
node clean-css.cjs
```

## Как это работает

Скрипт анализирует HTML файл и извлекает все используемые CSS классы, затем фильтрует CSS файл, сохраняя только необходимые правила.

### Алгоритм:
1. **Парсит HTML** - находит все `class=""` и `data-class=""` атрибуты
2. **Извлекает классы** - собирает уникальный список используемых селекторов
3. **Фильтрует CSS** - сохраняет только правила с найденными селекторами
4. **Сохраняет важные стили** - оставляет `:root`, `html`, `body`, `@layer`, `@property` и другие базовые правила

## Сравнение инструментов

| Инструмент | Строк в результате | Сокращение | Статус |
|------------|-------------------|------------|--------|
| Оригинал | 2,958 | - | - |
| PurgeCSS | 1,022 | ~65% | Работает, но много лишнего |
| **UnCSS** | **610** | **~79%** | ✅ **Лучший результат** |
| Простой анализатор | 2,853 | ~5% | Слабая очистка |

**Рекомендация**: Используйте UnCSS для максимального сокращения CSS! 🚀

## Файлы

- `clean-css.cjs` - основной скрипт очистки (простой анализатор)
- `clean-css-final.cjs` - версия с PurgeCSS (дает ~1022 строки)
- `clean-css-purge.cjs` - экспериментальная версия
- `clean-css-uncss.cjs` - версия с UnCSS (дает ~610 строк, но имеет проблемы с парсингом)

## Настройка

Если нужно изменить пути к файлам, отредактируйте переменные в начале `clean-css.cjs`:

```javascript
const htmlFile = path.join(__dirname, 'dist/html/index.html');
const baseCssFile = path.join(__dirname, 'dist/html/assets/base.css');
const outputCssFile = path.join(__dirname, 'dist/html/assets/clean.css');
```