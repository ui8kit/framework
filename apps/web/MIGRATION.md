# Миграция компонентов с app-starter → apps/web

## ✅ Завершено

Успешно выполнена полная адаптация и перенос компонентов из старой версии приложения (.project/app-starter) в новую архитектуру (apps/web) с соблюдением правил ui8kit.

## 📁 Структура проекта после миграции

### apps/web/src/components/
Переиспользуемые компоненты приложения, соответствующие ui8kit архитектуре.

```
components/
├── cards/
│   ├── PostCard.tsx           ✨ Карточка поста (2 варианта: top/default)
│   ├── SmallMediaCard.tsx      ✨ Компактная карточка с изображением
│   ├── CategoryCard.tsx        ✨ Карточка категории с кнопкой
│   ├── AuthorCard.tsx          ✨ Карточка автора с аватаром
│   └── AuthorBio.tsx           ✨ Блиц-биография
├── lists/
│   ├── CategoryList.tsx        ✨ Список категорий со счетчиком
│   └── TagList.tsx             ✨ Список тегов (badges)
├── SearchBar.tsx               ✨ Поиск с навигацией
├── PopularPosts.tsx            ✨ Популярные посты (контекст)
├── RecentPosts.tsx             ✨ Недавние посты (контекст)
├── HomeLatest.tsx              ✨ Последние посты (контекст)
├── NewsletterSignup.tsx        ✨ Рассылка (улучшена семантика)
├── Pagination.tsx              ✨ Пагинация
├── SEO.tsx                     ✨ SEO + JSON-LD
├── index.ts                    ✨ Центральный экспорт
└── README.md                   📖 Документация
```

### apps/web/src/partials/
Переиспользуемые части макета (шапка, подвал, сайдбар).

```
partials/
├── Header.tsx                  ✨ Шапка с навигацией и темой
├── Footer.tsx                  ✨ Подвал со ссылками
├── Sidebar.tsx                 ✨ Сайдбар контейнер
├── README.md                   📖 Документация
```

### apps/web/src/layouts/
Главные макеты приложения.

```
layouts/
├── MainLayout.tsx              ✨ Главный макет (3 режима)
├── DashLayout.tsx              📦 Макет админ-панели
└── README.md                   📖 Документация
```

## 🔄 Ключевые преобразования

| Что было | Что стало | Улучшение |
|---------|-----------|-----------|
| ❌ `order`, `align`, `ta`, `fw`, `c` | ✅ `fontSize`, `fontWeight`, `textColor`, `textAlign`, `items` | Семантические пропсы ui8kit |
| ❌ `cols="1-4"` | ✅ `grid="cols-4"` | Правильная Grid API |
| ❌ Без `data-class` | ✅ Полное покрытие `data-class` | CSS генерация |
| ❌ Inline styles | ✅ Только семантические пропсы | Чистота кода |
| ❌ Жесткие логики | ✅ Гибкие props | Переиспользуемость |
| ❌ Старый MainLayout | ✅ Новый MainLayout (3 режима) | Архитектура |

## 🎨 Семантические пропсы (ui8kit)

Все компоненты используют правильные ui8kit пропсы:

```tsx
// ✅ Правильно
<Title fontSize="xl" fontWeight="bold" textColor="foreground" data-class="title">
<Stack gap="6" items="center" py="8">
<Group justify="between" items="center">
<Button variant="primary" size="lg">

// ❌ Неправильно (старый код)
<Title size="xl" fw="bold" c="foreground">
<Stack gap="lg">
<Group align="center">
<Button className="bg-blue-500">
```

## 📦 Компоненты для документации

Все компоненты готовы к использованию в документации и служат основой для shadcn-style пакета:

✅ **PostCard** — адаптивная карточка поста с медиа и категориями
✅ **SmallMediaCard** — компактная карточка для боковых панелей
✅ **CategoryList** — список категорий с ссылками
✅ **TagList** — облако тегов (badges)
✅ **NewsletterSignup** — красивая форма подписки
✅ **Pagination** — навигация между страницами
✅ **SearchBar** — интегрированный поиск
✅ **Header** — адаптивная шапка с темой
✅ **Footer** — модульный подвал
✅ **MainLayout** — гибкий макет (3 режима)

## 🚀 Использование

### В App.tsx

```tsx
import { MainLayout } from '@/layouts/MainLayout'
import { SearchBar, CategoryList, PopularPosts } from '@/components'
import { menu } from '@/~data/wpfasty/context'

const navItems = menu.primary.items.map(item => ({
  id: `nav-${item.id}`,
  title: item.title,
  url: item.url,
}))

export default function App() {
  return (
    <MainLayout navItems={navItems} sidebar={<Sidebar />}>
      <Outlet />
    </MainLayout>
  )
}
```

### В страницах

```tsx
import { HomeLatest, RecentPosts } from '@/components'

export function HomePage() {
  return (
    <Stack gap="8">
      <HeroBlock />
      <HomeLatest />
      <RecentPosts />
    </Stack>
  )
}
```

## 📖 Документация

Каждая папка содержит подробный README:

- 📄 `components/README.md` — использование компонентов
- 📄 `partials/README.md` — использование партиалов
- 📄 `layouts/README.md` — использование макетов

## 🔧 Инструменты

✅ **Лиинт** — пройден успешно (npm run lint)
✅ **Dev сервер** — запущен (http://localhost:5173)
✅ **TypeScript** — все типизировано
✅ **Экспорты** — центральные индексы для каждого уровня

## 📝 CSS Generation

Все компоненты имеют правильные `data-class` для генерации CSS:

```
data-class="post-card"
data-class="post-card-image"
data-class="post-card-title"
data-class="post-card-excerpt"
data-class="post-card-categories"
data-class="post-card-footer"
```

Это позволяет легко генерировать и стилизировать CSS через tools/generator.

## 🎯 Следующие шаги

1. **Интегрировать реальные данные** — подключить контекст к компонентам
2. **Создать примеры** — добавить примеры использования в routes
3. **Документация** — создать Storybook/docs для компонентов
4. **Экспорт** — создать shadcn-style пакет для docs
5. **Стилизация** — добавить CSS через generator

## 📚 Файлы для просмотра

```
e:\_@Bun\@ui8kit-framework\apps\web\src\
├── components/            ← Новые компоненты ✨
├── partials/              ← Новые партиалы ✨
├── layouts/MainLayout.tsx ← Новый макет ✨
├── App.tsx                ← Обновлен ✨
└── ...
```

Миграция завершена успешно! 🎉
