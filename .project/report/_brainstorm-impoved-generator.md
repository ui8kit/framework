Давай поговорим как улучшить алгоритм генератора чтобы он автоматически распозновал изменения макетов и partials

сейчас генерация html жестко завязана на макет в шаблонах  @packages/generator/templates 

у нас есть линтер @packages/lint может быть перед началом генерации валидировать шаблон с реальным кодом приложения и выбрасывать предупреждение?

вот новый макет @apps/web/src/layouts/MainLayout.tsx 

вот части @apps/web/src/partials 

вот текущий сбой генератора

header.liquid не был сгенерирован хотя он есть @apps/web/src/partials/Header.tsx 

```
ℹ️  📝 Generating HTML pages...
❌    ❌ Failed to generate HTML for /: RenderError: ENOENT: Faile"p
artials/header.liquid" in "./views", file:E:\_@Bun\@ui8kit-framewoviews\layouts\layout.liquid, line:24, col:5
❌    ❌ Failed to generate HTML for /about: RenderError: ENOENT: ok
up "partials/header.liquid" in "./views", file:E:\_@Bun\@ui8kit-fr\web\views\layouts\layout.liquid, line:24, col:5
```

================================

Я сейчас в режиме Ask и не могу вносить изменения. Но давайте проанализирую проблему и предложу решение.

[3 tools called]

Теперь понял проблему. Посмотрю как генерируются partials:

[2 tools called]

Понял проблему! Конфиг настроен правильно, но есть несколько архитектурных проблем. Вот мой анализ:

## Проблема

1. **Partials не генерируются до HTML** — Генератор пытается рендерить HTML, который включает `{% include 'partials/header.liquid' %}`, но partials еще не созданы или процесс их создания упал.

2. **Layout template жёстко зависит от partials** — В `packages/generator/templates/layout.liquid` хардкод:
   ```liquid
   {% include 'partials/header.liquid' %}
   {% include 'partials/footer.liquid' %}
   ```

3. **Navbar в конфиге, но нет в layout** — Конфиг содержит `Navbar` в props, но layout не включает его.

## Рекомендуемое решение

### Вариант 1: Умный Layout Detection

Генератор должен:
1. Сканировать `src/partials/` для React компонентов
2. Сканировать `src/layouts/` для Layout компонентов
3. Автоматически генерировать `layout.liquid` на основе найденных компонентов
4. Валидировать что все `{% include %}` имеют соответствующие файлы

```typescript
// Новый сервис: TemplateValidatorService
interface TemplateValidation {
  layout: {
    hasHeader: boolean;
    hasFooter: boolean;
    hasNavbar: boolean;
    hasSidebar: boolean;
    customIncludes: string[];
  };
  partials: {
    available: string[];    // ['header', 'footer', 'sidebar']
    missing: string[];      // Включены в layout, но нет компонента
    orphaned: string[];     // Есть компонент, но не включен в layout
  };
}
```

### Вариант 2: Динамический Layout

Вместо жёстких `{% include %}` — генерировать layout на основе конфига:

```typescript
// generator.config.ts
html: {
  layout: {
    template: 'default', // или path к кастомному
    includes: {
      header: true,
      footer: true,
      navbar: false,
      sidebar: false,
    }
  },
  partials: { ... }
}
```

### Вариант 3: Интеграция с @packages/lint

Перед генерацией:
1. Lint проверяет соответствие layout и partials
2. Выбрасывает предупреждения о несоответствиях
3. Опционально авто-фиксит (добавляет/удаляет includes)

## Что делать сейчас

Для исправления текущей ошибки нужно:

1. **Изменить порядок генерации** — Сначала partials, потом layouts, потом HTML
2. **Добавить проверку существования** — Перед включением partial проверять что он есть
3. **Сделать layout.liquid гибким** — Использовать условные includes

Хотите, чтобы я переключился в Agent mode и реализовал эти улучшения?